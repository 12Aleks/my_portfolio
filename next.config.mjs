/** @type {import('next').NextConfig} */
const nextConfig = {};

export default nextConfig;

export const i18n = {
    locales: ['en', 'pl', 'de', 'ru', 'ua'],
    defaultLocale: 'pl',
    timeZone: 'Europe/Warsaw'
};

